# 签名申请、官方远程服务与第三方隔离方案评估

这份文档评估的是：如果未来要做“官方签名申请”“官方远程服务申请”“官方与第三方隔离”“第三方插件自动颁发/自动签发”等能力，当前方案应如何演进。

---

## 一、先明确当前状态

当前仓库已经具备的基础能力：

- 本地密钥生成
- 本地 manifest 签名
- 公钥验签
- 文件型 trust store
- 内联公钥 trust store
- 签名策略控制（严格 / 开发例外 / 允许未签名）

当前尚未具备的能力：

- 官方签名 CA / 官方远程签名服务
- 第三方插件身份注册中心
- 插件发布审批流
- 证书吊销 / key rotation 策略中心
- 插件发布域隔离（官方 / 第三方 / 私有）
- 自动签发 / 自动审核 / 自动续签

所以，如果要做“官方远程服务申请”和“自动颁发”，本质上是在当前本地签名方案之上，增加一层 **身份、审批、签发、隔离、吊销** 的平台能力。

---

## 二、建议的总体架构

建议把未来签名体系拆成 4 层：

### 第 1 层：插件身份注册层

职责：

- 注册插件作者 / 发布者身份
- 注册插件项目唯一 ID
- 绑定组织、团队、租户、来源
- 绑定插件类型（官方 / 第三方 / 私有）

建议字段：

- publisherId
- organizationId
- pluginId
- pluginOwnershipType
- contact
- reviewStatus

### 第 2 层：签名申请与审批层

职责：

- 插件作者提交签名申请
- 平台决定是否允许签发官方信任身份
- 管理审核流、风控、人工审批、自动审批

建议区分三类申请：

1. **官方插件申请**
   - 由平台官方团队维护
   - 进入官方 trust domain

2. **第三方插件申请**
   - 由外部开发者提交
   - 进入第三方 trust domain

3. **私有/企业内插件申请**
   - 面向单租户或私有部署
   - 不进入公共官方生态

### 第 3 层：签发与信任层

职责：

- 为通过审核的插件签发可验证身份
- 发布公钥 / 证书 / 信任链
- 支持轮换、吊销、过期

建议不要停留在“单个 PEM 文件人工分发”这一级，而应升级到：

- 官方根信任锚（root trust anchor）
- 域级签发者（official issuer / third-party issuer / tenant issuer）
- 插件签发身份（plugin signing identity）

### 第 4 层：运行时策略执行层

职责：

- 根据插件来源域执行不同策略
- 决定允许哪些插件安装、升级、调用
- 执行签名验证、吊销检查、来源隔离

---

## 三、官方与第三方隔离机制，建议怎么做

### 1. 至少划分 3 个信任域

建议把插件来源域明确拆成：

- **official**：官方插件
- **third-party**：第三方生态插件
- **private**：租户私有插件 / 企业内插件

### 2. 不同信任域使用不同 issuer

不要让官方和第三方共用同一组签发 key。

建议：

- official issuer key
- third-party issuer key
- private/tenant issuer key

这样做的好处：

- 官方与第三方天然隔离
- 某个第三方域出问题，不会污染官方域
- 策略可以按 trust domain 分层执行

### 3. manifest 中增加来源身份字段

如果以后要平台化，建议 manifest 或其扩展元数据中增加：

- publisherId
- trustDomain
- issuedBy
- certificateId 或 signingIdentityId
- issuedAt / expiresAt

### 4. 运行时策略按来源域区分

例如：

- official：默认允许安装
- third-party：需显式批准或沙箱更严格
- private：仅允许指定租户 / 指定组织加载

---

## 四、第三方插件“自动颁发”如何设计

### 可以做，但不能做成完全无约束的自动签发

建议采用 **分级自动化**：

#### 级别 A：注册即获开发级身份

特点：

- 自动签发开发环境身份
- 仅能用于开发或灰度环境
- 不进入 official trust domain

适合：

- 第三方作者快速开始集成

#### 级别 B：通过基础校验后自动签发第三方生产身份

自动检查项可包括：

- 插件 manifest 完整性
- 能力声明合法性
- 是否通过基础静态扫描
- 是否满足包结构规范
- 是否带签名申请元数据

满足后：

- 自动签发 third-party 域身份
- 不等于官方认证

#### 级别 C：人工审核后升级为官方推荐 / 官方托管身份

特点：

- 需要安全审核、合规审核、质量审核
- 进入更高信任等级
- 可以显示为官方 / 推荐插件

### 结论

“自动颁发”可以做，但建议限定在：

- 开发级身份自动签发
- 第三方生产级身份的受控自动签发
- 官方身份坚持人工审批或更严格流程

---

## 五、签名申请如果要做官方远程服务，建议接口怎么分

建议拆成 4 组 API：

### 1. 插件注册 API

用途：

- 注册 publisher
- 注册 pluginId
- 声明插件语言、类型、运行时、能力范围

### 2. 签名申请 API

用途：

- 提交待签发的 manifest 或摘要
- 指定申请域（official / third-party / private）
- 返回申请状态

### 3. 证书 / 公钥分发 API

用途：

- 拉取 issuer 公钥
- 拉取 trust bundle
- 拉取吊销列表 / 状态

### 4. 签名校验策略 API

用途：

- 拉取当前生效的 trust policy
- 判断某类插件在当前租户是否允许安装或加载

---

## 六、从当前本地方案演进到平台方案，建议的实现顺序

### 阶段 1：本地增强

先在当前仓库里补这些能力：

- trust domain 字段建模
- issuer / publisher 元数据建模
- trust bundle 文件格式
- 吊销列表文件格式

### 阶段 2：离线签发中心

实现一个本地或内网签发工具：

- 注册插件
- 生成签发记录
- 输出 trust bundle
- 输出签名过的 manifest

### 阶段 3：远程签发服务

实现服务端：

- 插件注册
- 提交申请
- 审核状态流转
- 签发第三方身份
- 分发 issuer 公钥和策略

### 阶段 4：运行时联动

让 `ExtensionManager` 在加载时支持：

- 读取 trust bundle
- 验证 issuer 链
- 检查吊销状态
- 按 trust domain 执行不同策略

---

## 七、对你这个项目的具体建议

如果目标是未来支持官方生态和第三方生态，我建议不要直接把当前 `trusted-keys/<keyId>.pem` 当成最终形态。

更合适的演进方向是：

### 短期保留

- 继续兼容 `trusted-keys/<keyId>.pem`
- 继续兼容 inline PEM

### 中期增加

- trust bundle（可包含多个 issuer、公钥、域、用途）
- plugin publisher 元数据
- trust domain / issuer 信息
- 吊销列表

### 长期平台化

- 官方签名申请中心
- 第三方签发域
- 官方与第三方分离的 issuer
- 自动化申请 + 人工升级审批
- 租户级策略和隔离

---

## 八、核心结论

### 1. 官方与第三方一定要隔离

至少要做到：

- 不同 trust domain
- 不同 issuer key
- 不同策略

### 2. 自动颁发可以做，但不能等同于官方认证

建议：

- 自动签发第三方开发 / 基础生产身份
- 官方身份必须更高门槛

### 3. 当前签名认证方式适合作为基础层

当前的：

- 文件公钥模式
- 内联公钥模式

很适合作为底层能力，但如果要做官方远程服务，需要再往上叠加：

- 身份
- 审批
- trust domain
- issuer 链
- 吊销
- 策略

---

## 九、建议下一步

如果要继续实施，而不是只做评估，下一步最值得先做的是：

1. 为 manifest 建模 `publisherId / trustDomain / issuedBy`
2. 设计 `trust-bundle.json` 格式
3. 设计 `revocation-list.json` 格式
4. 让 `ExtensionManager` 支持读取 trust bundle
5. 再决定是否引入远程签发服务
